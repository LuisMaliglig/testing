export const awsConfig = {
    region: "ap-southeast-1", // Change to your AWS region
    apiKey: "", // Use the API key string
    mapName: "explore.map.Grab",
    routeCalculator: "explore.route-calculator.Grab",
    placeIndex: "explore.place.Grab"
  };

export const mapboxConfig = {
  accessToken: "",
};